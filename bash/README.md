# bash-it custom configuration

## instllation

```bash
git clone https://github.com/musou1500/bash-config ~/.config/bash
```

And add `export BASH_IT_CUSTOM=~/.config/bash` to proper line in `~/.bash_profile`
